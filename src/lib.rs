pub mod canvas;
pub mod color;

pub mod prelude {
    pub use crate::canvas::Canvas;
    pub use crate::color::Color;
}

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
